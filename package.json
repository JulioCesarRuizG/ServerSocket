{
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.1",
    "ws": "^8.13.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "start": "node index.js",
    "build": "npm install && ng build"
  }
}
